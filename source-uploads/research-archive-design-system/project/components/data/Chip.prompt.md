Inline monospace code token — paths, commands, env vars, commit hashes.

```jsx
Run <Chip>env | grep -i github</Chip> to check, or read <Chip>engineering/.mcp.json</Chip>.
```

No variants. Sits inline in prose and table cells.
