Centered mono capsule on a hairline rule — marks a model swap or system-level break between turns.

```jsx
<ModelSwitchDivider command="/model claude-opus-4-8" note="— switched by the Operator." />
```

Props: `command` (mono), `note` (sans, faint), `icon` (default solar:settings-linear).
