# Canonicalized design references

These references are sanitized historical source material, not alternate product implementations.

The two unique wheel archives were inspected in temporary staging. Files already canonical in `app/` or common to both archives were deduplicated. Only unique public-safe source remains under `wheel-v1/`; `wheel-v2/` contains its disposition receipt because its implementation source is already canonical in `app/`.

The research archive reference retains generic components and tokens only. Raw transcript examples and remote dependencies are not published.
