# Wheel prototype v2

The second unique archive's two distinct implementation files are retained here because `app/` now contains the canonical TypeScript product. Its shared historical scaffold is retained once under `wheel-v1/`; the stylesheet's remote font request is removed in the sanitized edition.

The ZIP container remains removed, and common scaffold files are not duplicated between wheel editions.
