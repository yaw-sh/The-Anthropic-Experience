# Preserved Claude artifact

`the-anthropic-experience.jsx` is the supplied Claude artifact, copied byte-for-byte for design reference and provenance. It is intentionally outside the application runtime and is not imported or served by `app/`.

Run `npm run check:root` from `anthropic-experience/app/` to verify the repository layout and that this preserved artifact is tracked and unchanged from `HEAD`.
