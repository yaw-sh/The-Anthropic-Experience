# Design sources and references

`source-images/` contains the ten delivered scene images named by the authoritative root blueprint. Product integration and generated derivatives belong to the product-build track.

`reference/` contains public-safe canonicalized source references:

- `wheel-v1/` retains only the two source files unique to the first wheel archive.
- `wheel-v2/` records that the second archive’s unique implementation files already live in `app/`.
- `research-archive-system/` retains generic reusable components and tokens after raw transcript previews, generated bundles, remote previews, and duplicate files were removed.

No ZIP container or archive binary remains in the current tree.
