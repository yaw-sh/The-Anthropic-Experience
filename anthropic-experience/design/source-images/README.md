# Scene source images

Ten delivered wide JPEG scene images remain available to the product-build track under the stable filenames defined in the root master blueprint. This cleanup track does not alter, optimize, or integrate them into `app/`.
