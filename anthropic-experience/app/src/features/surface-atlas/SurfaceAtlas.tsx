import { SurfaceDirectoryExperience } from "../surface-directory/SurfaceDirectoryExperience";

export function SurfaceAtlas() {
  return (
    <main id="main-content" className="page-shell atlas-page">
      <SurfaceDirectoryExperience fullFilters />
    </main>
  );
}
